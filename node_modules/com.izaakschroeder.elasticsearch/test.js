
var es = require('./elasticsearch');

module.exports = {

	setUp: function(callback) {
		this.client = es.createClient();
		this.index = this.client.index("node-unit-test");
		callback();
	},

	tearDown: function(callback) {
		callback()
	},

	createIndex: function(test) {
		this.index.create()
	},

	describeIndex: function(test) {

	},

	deleteIndex: function(test) {

	}


}