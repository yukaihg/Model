= Elastic Search =

This package contains Elastic Search bindings for node.js.

{{{
$ npm install com.izaakschroeder.elasticsearch
}}}

{{{
	var client = require('com.izaakschroeder.elasticsearch').createClient();
	var doc = client.index('i').mapping('users').document('test');
}}}